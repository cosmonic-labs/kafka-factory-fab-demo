# Scrapes Redpanda's public metrics (the set its Grafana dashboard reads).
global:
  scrape_interval: 5s
scrape_configs:
  - job_name: redpanda
    metrics_path: /public_metrics
    static_configs:
      - targets: ["redpanda:9644"]
