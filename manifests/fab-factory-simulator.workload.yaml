apiVersion: runtime.wasmcloud.dev/v1alpha1
kind: Workload
metadata:
  name: "fab-factory-simulator"
  namespace: default
  # Tags: the Workloads grid searches label keys and values, so "fab-factory"
  # (or "station=st02") filters the line's workloads on screen.
  labels:
    app.kubernetes.io/part-of: fab-factory
    app.kubernetes.io/name: fab-factory-simulator
    app.kubernetes.io/version: "0.1.0"
    fab.meridian.example/station: simulator
spec:
  hostInterfaces:
    - namespace: cosmonic
      package: kafka
      version: 0.5.0
      interfaces: [consumer, producer]
      config:
        bootstrap.servers: 127.0.0.1:9092
        broker.address.family: v4
        # Grant: the control topic it consumes, every input topic it plays
        # onto, and line.metrics for its produced counts and heartbeat.
        topics: "sim.control,probe.lots,dieattach.readings,wirebond.raw,inspect.jobs,mold.telemetry,finaltest.bins,line.metrics"
        consumer.group.id: "factory-simulator"
        auto.offset.reset: latest
        enable.auto.commit: "false"
  # A wasi:cli/run component runs as the workload's long-running service
  # (Desktop drives `run` only under spec.service; docs/DECISIONS.md).
  service:
    name: simulator
    image: ghcr.io/cosmonic-labs/kafka-factory-fab-demo/fab-factory-simulator:0.1.0@sha256:97b75ead48405688f6a628ec5f425502ddf95447bf406154e60c62de040d7508
    maxRestarts: 100
    localResources:
      allowedHosts: []
  # Desktop's Workload schema requires at least one component; this entry is
  # never invoked (the service above is what runs).
  components:
    - name: simulator
      image: ghcr.io/cosmonic-labs/kafka-factory-fab-demo/fab-factory-simulator:0.1.0@sha256:97b75ead48405688f6a628ec5f425502ddf95447bf406154e60c62de040d7508
      localResources:
        allowedHosts: []
